<%= form_for([@league, @league.teams.build]) do |f| %>
    <% if @team.errors.any? %>
        <div id="error_explanation">
            <h2>
                <%= pluralize(@team.errors.count, "error") %> prohibited this article from being saved:
            </h2>
            <ul>
                <% @team.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <p>
        <%= f.label :name %><br>
        <%= f.text_field :name %></br>
    </p>

    <p>
        <%= f.label :players %><br>
        <%= f.text_area :players %>
    </p>
    <p>
        <%= f.submit %>
    </p>
<% end %>
